---
layout: base.njk
bodyClass: home
---
{# 首页不走 Markdown：它是全站交互最多的一页。简介直接写在这里，
   头像换成 src/images/ 下你自己的图。 #}
<section class="hero">
  <div class="hero-left">
    <img class="avatar" src="{{ '/images/logo-tp.png' | asset }}" alt="头像">
  </div>
  <div class="intro">
    <p>本站是「淡紙 ThinPapers」，一个基于 Eleventy 的极简中文写作模板。</p>
    <p>文章以 Markdown 撰写，置于 <code>src/posts/</code> 目录下。标题取自正文的第一个一级标题，列表、合集与时间线均在构建期生成，无需在 front matter 中重复填写。</p>
    <p>本页由 <code>src/index.njk</code> 渲染，这段简介可替换为你要展示的内容。</p>

    <p>使用 11ty，希望 SSG 的构建不依赖模板语言，以及轻量 <a class="ssg-icon" href="https://www.11ty.dev" rel="noopener" target="_blank" title="Eleventy"><img src="{{ '/images/T3q0klu3ui-120.avif' | asset }}" alt="11ty"></a></p>

    {# 社交链接：单色 svg 走 mask 染色，跟随主题文字色 #}
    <div class="social-links">
      {%- for s in site.social %}
      <a class="social-link" href="{{ s.url }}" aria-label="{{ s.name }}" title="{{ s.name }}"{% if s.url %} target="_blank" rel="noopener"{% endif %}>
        <span class="social-icon" style="--social-icon: url('{{ s.icon | asset }}')"></span>
      </a>
      {%- endfor %}
    </div>
  </div>
</section>

{%- set tlYears = collections.post | byYear %}
<a class="timeline-btn" href="{{ (tlYears[0] and '/timeline/' + tlYears[0].year + '/' or '/timeline/') | url }}"><span>—— 时间线 ——</span></a>

{# 合集列表：来自 src/_data/topics.json #}
<ul class="collection-list">
  {%- for t in topics %}
  <li><a href="{{ ('/collections/' + t.slug + '/') | url }}">{{ t.name }}</a></li>
  {%- endfor %}
</ul>

{# 插图署名：小鸡插图来自 Haskell 教程书，版权不随本站的 MIT 许可，来源要写明。
   换成自己的图时，这一整段可以删掉。 #}
<p class="site-credit">头像与 favicon 的小鸡插图来自 <a href="https://learnyouahaskell.github.io/starting-out.html" rel="noopener">《Learn You a Haskell for Great Good!》</a>，版权归原作者所有。</p>
